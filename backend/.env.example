# Optional: the dashboard lets you paste your OpenRouter API key directly in
# the browser, so this file isn't required. Kept here in case you prefer to
# wire up a server-side default via python-dotenv.
OPENROUTER_API_KEY=sk-or-v1-your-key-here
